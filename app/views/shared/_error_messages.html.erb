<% if @relative.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t('.er_text') %> <%= pluralize(@relative.errors.count, t('.er')) %>.
    </div>
    <ul>
    <% @relative.errors.full_messages.each do |msg| %>
    <div class="alert alert-danger">
      <li><%= msg %></li>
    </div>
    <% end %>
    </ul>
  </div>
<% end %>
