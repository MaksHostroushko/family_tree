<% provide(:title, 'All users') %>
<h1 class="h1_us_index"><%= t('.text') %></h1>

<div class="users_main col-md-10 row">
    <% if @users.present? %>
      <%= render @users %>
    <% else %>
      <h2 class="col-10 text-center"><b>Sorry</b></h2>
    <% end %>
</div>
